# Callbacks for neural model (autoencoder) training.
# early_stopping requires a val split (splitter: stratified_kfold).
# Remove or comment out if using splitter: none.

early_stopping:
  _target_: lightning.pytorch.callbacks.EarlyStopping
  monitor: val/loss
  patience: 10
  mode: min
  verbose: false
